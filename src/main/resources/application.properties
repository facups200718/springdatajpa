# MySQL connection properties
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=root
spring.datasource.password=123Huellita$
spring.datasource.url=jdbc:mysql://localhost:3306/CompanyDB?createDatabaseIfNotExist=true&useSSL=false

# Log JPA queries
# Sirve para loggear las queries (y que por ej se vean en la consola)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Drop and create new tables (create, create-drop, validate, update)
# Sirve para, en este caso, indicar a hibernate que dropee y cree las tablas cada vez
spring.jpa.hibernate.ddl-auto=create-drop

# Hibernate SQL dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
